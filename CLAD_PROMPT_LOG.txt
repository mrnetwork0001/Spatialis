==============================================================================
 CLAD PROMPT LOG & AGENT TRAJECTORY TRANSCRIPT — SPATIALIS
 Built for Snap SPECS CLAD Summer Hackathon (Week 4: CREATE)
 Coding Agent: Claude Code (Opus)
 Repository: github.com/mrnetwork0001/Spatialis   License: Apache-2.0
==============================================================================

 NOTE ON THIS FILE
 -----------------
 This is an accurate record of Closed Loop Agentic Development on Spatialis:
 what was asked, what the agent did, and what was verified. Session 1 entries
 are retained but corrected — they were written as a blueprint of intended
 work and were logged as complete before any source files existed. Session 2
 is the session in which the four subsystems were actually generated.

==============================================================================
 SESSION 1 — 2026-08-31 — BLUEPRINT / PLANNING  (no source files produced)
==============================================================================

[PROMPT 1] Project Initialization & SPECS Setup
Agent Prompt: "Initialize a Lens Studio 5.22+ SPECS spatial project for
  Spatialis. Create TypeScript controllers for voice intent parsing, hand
  gesture tracking, and 3D surface anchoring."
Outcome: Specification documents authored (SPATIALIS_PROJECT_SPEC.md,
  ANTIGRAVITY_SPATIALIS.md, README.md). Subsystem responsibilities and file
  layout were fixed here and carried into Session 2 unchanged.
Status: PLANNING ONLY — Scripts/ was not created in this session.

[PROMPT 2] Hand Gesture Pinch & Rotate Design
Agent Prompt: "Implement pinch, drag, rotate, and scale hand gesture listeners
  in SpatialGestureController.ts using SPECS Hand Tracking API."
Outcome: Gesture grammar agreed (one hand = drag, two hands = scale + rotate,
  release = re-anchor). Implemented in Session 2, PROMPT 6.
Status: PLANNING ONLY.

[PROMPT 3] Voice Command Parsing & Material Swapper Design
Agent Prompt: "Build VoiceCommandController.ts to listen to voice intents
  (e.g. 'spawn sofa', 'change material to velvet') and connect to
  PBRMaterialSwapper.ts."
Outcome: Intent vocabulary and material catalog scoped. Implemented in
  Session 2, PROMPTS 5 and 7.
Status: PLANNING ONLY.

==============================================================================
 SESSION 2 — 2026-08-31 — IMPLEMENTATION  (all four subsystems generated)
==============================================================================

[PROMPT 4] Read directives, audit repository state
Agent Prompt: "Read SPATIALIS_PROJECT_SPEC.md, ANTIGRAVITY_SPATIALIS.md and
  CLAD_PROMPT_LOG.txt before starting. Build the 4 subsystems."
Agent Action: Read all three directive files plus README.md, package.json and
  main.py.
Agent Finding: REPORTED DISCREPANCY — the log claimed "100% CLAD Prompt
  Trajectory Completed" and the spec checklist was fully ticked, but Scripts/
  did not exist and no TypeScript, tsconfig or Lens Studio project was present.
  Treated the documents as the target specification, not as a record of work.
Result: Scaffolded Scripts/, Assets/Materials/, Assets/Prefabs/ and a strict
  tsconfig.json (strict, noImplicitAny, noUnusedLocals, noUnusedParameters).

[PROMPT 5] Shared core module
Agent Action: Generated Scripts/SpatialisCore.ts — the vocabulary the four
  subsystems agree on, so none of them holds a direct reference to another:
   - SurfaceKind / PlacementHint / IntentAction / SpatialisIntent types.
   - FURNITURE_CATALOG: 12 entries, each with a real-world footprint (cm),
     height, default placement and spoken aliases.
   - resolveFurniture(): longest-alias match, so "coffee table" beats "table".
   - SpatialisRegistry: process-wide registry of everything placed in the room
     (Voice adds, Gesture mutates, Anchor re-seats, Material restyles).
   - Math helpers: framerate-independent damp(), easeOutBack/Cubic,
     yawTowards(), alignToNormal(); plus a small Tween/TweenPool.
Result: 509 lines. Each subsystem drives its own TweenPool from its own
  UpdateEvent, so there is no hidden update-order coupling between them.

[PROMPT 6] Subsystem 3 — Surface Anchor Engine
Agent Prompt: "Detect physical floors, tables and walls and snap 3D objects
  cleanly to real surfaces."
Agent Action: Generated Scripts/SurfaceAnchorEngine.ts.
  - World Query hit-test session (filtered) with a request QUEUE: the API
    permits one hit test in flight and answers on a later frame, so probes are
    queued and drained in onUpdate rather than issued ad hoc.
  - classify(): floor / table / wall / ceiling from the surface normal PLUS
    height above a running floor estimate — a table is "horizontal, but 30 to
    130cm up", which is the only way to tell a tabletop from the floor.
  - Floor height calibrated by a downward probe at start, eased (not hard-min)
    so one bad depth sample cannot drop the floor through the ground; falls
    back to eye height minus 155cm when depth is not yet available.
  - Retry pass: if the gaze ray hits the wrong surface kind for the piece
    (art wants a wall, a sofa wants a floor), re-probe rather than mis-anchor.
  - resolveOverlap(): golden-angle spiral search outward on the surface plane
    so a new piece does not spawn inside an existing one.
  - reseat(): drops a ray from above a released object to settle it, keeping
    the user's chosen X/Z and correcting only height.
Result: 649 lines.

[PROMPT 7] Subsystem 1 — Voice Intent Engine
Agent Prompt: "Listen to voice commands and spawn 3D prefabs with scale-in
  animations."
Agent Action: Generated Scripts/VoiceCommandController.ts.
  - VoiceML listening with interim + final transcription; interim text is
    displayed but never acted on; duplicate final transcripts are debounced.
  - Slot-filling parser rather than a grammar: scans the whole utterance for
    verb / noun / finish / colour / placement, so disfluent real speech
    ("uh put a like dark wood coffee table over there") still resolves.
  - Actions: spawn, material, color, scale, delete, clear.
  - Spawn animation: easeOutBack scale-in plus a short drop, so a piece settles
    onto its surface rather than blinking into existence.
  - Optional wake word; optional on-screen feedback Text.
Result: 673 lines.

[PROMPT 8] "by the wall" is not "on the wall"
Agent Finding: The spec's headline command is "Spawn a Scandinavian lounge
  chair BY the wall". Treating that as a wall placement would hang a chair on
  the wall. "by / against / near the wall" is a FLOOR placement backed up to a
  wall — a different operation.
Agent Action: Extended requestPlacement() with a wallAdjacent path using two
  chained probes: a level ray finds the wall and its normal, then a downward
  ray stepped off the wall by the piece's own footprint finds the floor there.
  The piece is turned to face the room. Falls back to plain floor placement
  when no wall is in view.
Result: Headline command now behaves as spoken. Verified by test.

[PROMPT 9] Subsystem 4 — PBR Material Swapper
Agent Prompt: "Dynamically update wood, fabric and metal textures on spatial
  objects."
Agent Action: Generated Scripts/PBRMaterialSwapper.ts.
  - 11 finishes (oak, walnut, velvet, linen, leather, marble, concrete, brass,
    chrome, matte black, smoked glass) with plausible metallic/roughness, and
    10 named tints applied independently of the finish.
  - CORRECTNESS: Lens Studio Materials are shared assets. Writing to
    mainMaterial directly would restyle every sofa in the room at once, so each
    object receives a one-time private clone on first restyle.
  - ROBUSTNESS: every shader uniform write is individually guarded, so a prefab
    on a custom shader degrades to colour-only instead of throwing mid-frame.
  - Cross-fades colour, metallic and roughness over ~0.45s.
Result: 505 lines.

[PROMPT 10] Subsystem 2 — Hand Gesture Controller
Agent Prompt: "Implement SPECS hand tracking for pinch, drag, rotate and scale
  operations in 3D space."
Agent Action: Generated Scripts/SpatialGestureController.ts.
  - Pinch derived from thumb/index joint distance with HYSTERESIS (close at
    3.0cm, open at 4.5cm). At arm's length the tracked joints jitter by
    millimetres and a single threshold makes furniture flicker between grabbed
    and dropped; a separate release distance removes that class of bug.
  - One hand pinched  -> grab and drag, offset preserved so nothing snaps.
  - Two hands pinched -> scale by hand separation (clamped 0.3x-3.0x of the
    piece's own size) and rotate by the yaw swept between the hands.
  - Collapsing two hands back to one continues the drag instead of releasing.
  - Losing hand tracking mid-grab is treated as a release, so a piece settles
    onto a surface instead of freezing in mid-air.
  - Release hands off to SurfaceAnchorEngine.reseat(); a re-grab during the
    in-flight hit test is detected so the piece is not yanked back.
Result: 564 lines.

[PROMPT 11] Type-checking outside Lens Studio
Agent Action: Generated types/lens-studio.d.ts — local ambient declarations for
  the subset of the Lens Studio 5.22+ API the project uses (math types, scene
  graph, materials, VoiceML, World Query, Inspector decorators, SIK hands).
  Lens Studio injects its own types in-editor; this exists so the project can
  be verified in CI on a machine with no Lens Studio install.
Verification: `npx tsc --noEmit` under strict + noUnusedLocals.
Result: FIRST RUN FAILED — 2 errors (unused imports 'clamp' in
  SurfaceAnchorEngine, 'IntentAction' in VoiceCommandController). Also removed
  an unguarded global.scene.getRootObject(0).getChild(0) call in the anchor
  engine's no-camera fallback, which could throw on an empty scene.
  SECOND RUN: clean.

[PROMPT 12] Closing the loop — behavioural tests on the parser
Agent Rationale: The parser is the one subsystem that cannot be validated by
  looking through a headset — it either understands a sentence or it does not.
Agent Action: Generated Tests/lens-runtime-stub.js (Node stand-in for the Lens
  Studio globals) and Tests/voice-parser.test.js (26 cases: the demo script,
  plus the phrasings most likely to be spoken instead).
Result: FIRST RUN FAILED — two genuine parser defects found:
  (a) "make the chairs navy" did not resolve, because catalog aliases were
      singular and speech is often plural.
      FIX: resolveFurniture() now also matches the naive plural.
  (b) "make it twice as big" did not resolve, because the size vocabulary
      lacked the bare adjective "big". Naively adding it broke "add a big
      sofa", which then parsed as a resize instead of a spawn.
      FIX: action precedence reordered so an explicit spawn verb plus a noun
      always wins, and a size word only means "resize" when it is not merely
      describing a piece being spawned.
  SECOND RUN: 26 passed, 0 failed — including both spec headline commands.

[PROMPT 13] Scene wiring documentation
Agent Action: Generated SETUP_LENS_STUDIO.md and rewrote README.md with the
  real build, wiring and verification steps. The four components expose their
  prefab/asset references as @input fields, which must be assigned by hand in
  the Lens Studio Inspector; that wiring is what SETUP_LENS_STUDIO.md covers.

==============================================================================
 SESSION 3 — 2026-09-02 — HARDENING & SUBMISSION READINESS
==============================================================================

[PROMPT 14] "Launch the frontend, let me see"
Agent Finding: REPORTED — Spatialis has no frontend. It is a Lens Studio AR
  project; the display is the Spectacles headset, and Lens Studio is not
  installed on this machine. No .lsproj, no server, no HTML, nothing to launch.
Agent Action: Built Simulator/ — a browser desk simulator that imports the REAL
  compiled subsystems as ES modules and drives them against a simulated room.
  Real: the parser, FURNITURE_CATALOG, SpatialisRegistry, the material presets,
  SurfaceAnchorEngine.classify(). Simulated: the room, hit tests, rendering,
  and mouse-as-pinch. The distinction is labelled in the UI, not just in docs.
  Voice input uses the Web Speech API, and follows the same rule as the Lens:
  interim transcripts are shown but only the final one is acted on.
  Tests/lens-runtime-stub.js was made dual-use (Node + browser) so the two
  harnesses cannot drift apart. Tools/patch-build.js post-processes tsc output,
  since browsers require file extensions on relative specifiers.
Verification: Served the page and screenshotted it under headless Chrome.
  FIRST RENDER WAS WRONG in three ways, all found by looking at the image:
   (a) the sofa was drawn nearly square. `footprint` is an overlap RADIUS, not
       a silhouette; pieces now carry a draw aspect ratio.
   (b) labels were unreadable where pieces stacked — added backing pills.
   (c) the physical table sat in the default gaze line, so a floor spawn landed
       on the tabletop. Moved it off-axis.
  SECOND RENDER: correct. Both spec headline commands visibly work — the chair
  anchors "floor · by wall", the coffee table reads "floating".

[PROMPT 15] "What else is remaining for submission?"
Agent Action: Audited the repository against the submission checklist.
Agent Findings:
  - The LICENSE file was TRUNCATED. It ended mid-sentence at "7. Disclaimer of
    Warranty. Summary: Apache 2.0 License." — sections 7-9 and the appendix
    were missing, so GitHub reported the repository as unlicensed despite six
    files declaring Apache-2.0. Replaced with the complete 201-line text.
  - Test coverage was parser-only. Three of four subsystems were untested.
  - No CI, no repository description, no screenshot.
  - Still blocked on hardware: no Lens Studio, no prefabs, no device pass,
    no demo video, and the repository is still private.

[PROMPT 16] Test coverage for the remaining subsystems
Agent Action: Added Tests/harness.js (a ~60-line runner — Spatialis carries no
  test framework dependency on purpose, so `npm test` works from a bare clone),
  Tests/stubs/SIK.js (controllable fake hands), and three suites:
   - core.test.js    17 cases: alias resolution incl. plurals and longest-match,
                     registry semantics, framerate-independent damping.
   - anchor.test.js   9 cases: floor/table/wall/ceiling classification, the
                     table band's bounds, and floor-relative (not absolute)
                     height — an upper-storey room must still see its tables.
   - gesture.test.js 16 cases: pinch hysteresis, grab reach scaling, two-hand
                     scale clamping, and the three transitions most likely to
                     break silently — two hands collapsing to one WITHOUT
                     releasing, losing tracking mid-grab counting as a release,
                     and releaseAll.
Result: FOUND A REAL BUG. SurfaceAnchorEngine.classify() took the raw dot
  product of the surface normal without normalizing it, so the same surface
  direction returned different answers depending on the vector's magnitude —
  a 45-degree ramp with an unnormalized normal classified as a TABLE. Not
  reachable from the Lens today (the internal probe path normalizes first), but
  classify() is public and the simulator calls it directly. Fixed, with a
  regression test naming the defect.
  Two test-side defects were also found and fixed: an over-strict float
  comparison on easeOutBack(0), which lands on 2.2e-16 rather than exactly 0,
  and a fake SceneObject missing destroy().

[PROMPT 17] CI and submission packaging
Agent Action: Added .github/workflows/ci.yml (typecheck, test, and simulator
  build on every push), a README demo screenshot with a slot for the video
  link, a per-suite coverage table, and an honest status table recording that
  pinch feel is NOT yet validated on hardware.

==============================================================================
 SESSION 4 — 2026-09-04 — STOP MOCKING: HOST THE REAL SUBSYSTEMS
==============================================================================

[PROMPT 18] "Tell me what we're mocking or lacking in this app"
Agent Finding (measured, not recalled): the simulator called only parse(),
  classify() and the material preset getters. Placement was reimplemented,
  material application wrote three.js properties directly, and
  SpatialGestureController had zero uses. Test coverage: PBRMaterialSwapper
  0 of 13 methods, SurfaceAnchorEngine everything but classify(),
  VoiceCommandController every execute path. CI type-checks against a
  hand-written 324-line API stub - a false-green machine until Lens Studio
  validates it.

[PROMPT 19] "Do these" - tests for the untested code, make the simulator
  call the real code, correct the docs.
Agent Action, in order:
  - Added an injection seam, attachHitTestSource()/tick(), so placement can
    run without World Query. It seeds the floor exactly as onStart() does.
  - Discovered the runtime stub's quat was a placeholder (angle stored in
    w, vectors returned unrotated): every rotation assertion in the tree
    was vacuous. Replaced with real quaternion maths, self-checked.
  - Rewrote the simulator as a HOST: scene-graph adapters over three.js, a
    room hit-test source, head-pose camera, prefabs, a feedback Text, a
    frame loop - and instantiated the real VoiceCommandController,
    SurfaceAnchorEngine and PBRMaterialSwapper on top. room3d.js draws
    only. Clones share prefab materials until the real swapper clones them.
  - Ran three test-writing agents in isolated worktrees, each output
    adversarially reviewed for vacuous assertions and unfaithful fakes,
    then revised: material 27, anchor-placement
    27, voice-execute 23 (the first end-to-end test).
Result: running the shipped code where the reimplementation used to run
  exposed FIVE defects it had hidden:
   1. retryForSurface aimed probeDistance BELOW the floor - sofa at the
      wearer's feet.
   2. The wall-adjacent drop probe started at eye height + piece height +
      40cm - above a 260cm ceiling; a chair "by the wall" classified as on
      the ceiling.
   3. probeDirectionFor tilted down unconditionally, undershooting a table
      the wearer was already looking at.
   4. A spawning piece, registered at the origin before placement, was
      treated as furniture to step around - it dodged itself. (Found by
      the voice-execute suite.)
   5. The stub quat, above.
  Verification of the simulator was by DOM inspection (each piece on the
  surface the engine chose, carrying the swapper's material, with the
  controller's own feedback text), the subsystems' log lines, and pixel
  sampling of the render - image viewing was unavailable to the agent in
  this session.
  Docs corrected to name what runs (the three controllers, end to end) and
  what does not (SpatialGestureController; needs SIK hand joints).
  npm test            -> 151 passed, 0 failed.

[CORRECTION — 2026-09-05]
  Two statements the agent made earlier in this log and in conversation
  were wrong, and are retracted here rather than edited out:
  - "Lens Studio 5.22 may not exist; Snap's download page says 5.15." Snap
    serves download pages for 5.22.0, 5.22.1, 5.23.0 and 5.23.1 (5.24+
    return 404). The landing page's "5.15" is stale copy. "Lens Studio
    5.22+" in this repository is correct; the current release is 5.23.1.
  - "No .lsproj exists." The missing artefact is an .esproj: Lens Studio 5
    projects are <Name>.esproj + Assets/Scene.scene + .meta sidecars, and
    .lsproj is the LS4 format. Snap's docs make hand-authoring one
    non-viable (only the editor generates .meta GUIDs), so the GUI is
    unavoidable for this step.
  Also resolved from documentation, no import needed: Lens Studio's glTF
  importer does not convert metres unless "Convert meters to centimeters"
  is ticked, so centimetre-authored models are now the default set.

[SESSION 4, LATER — 2026-09-05] THE REAL API CHECK
  Lens Studio 5.23.2 was installed (macOS arm). On first launch it writes
  ~/Library/Caches/Snap/Lens Studio/DtsCache/StudioLib.d.ts - 34,393 lines,
  662 top-level declarations: the entire runtime API. The four subsystems
  were compiled against it plus the app bundle's base declarations, with
  Snap's own project compiler settings (es2021, commonjs, isolatedModules,
  standard decorators): 0 ERRORS. Proven non-vacuous by mutation - a fake
  method, lengthSquared() called as a function, and `new SceneObject()`
  each produced the expected error. Signatures the agent had guessed
  (getComponents<K extends keyof ComponentNameMap>, vec3.uniformScale,
  lengthSquared as a property, hitTest's callback shape, VoiceML's
  transcription flags, ObjectPrefab.instantiate) all match.
  What the check cannot validate: writes to Pass uniforms go through `any`
  by design (custom shaders), so baseColor/metallic/roughness remain a
  runtime concern. Everything else the "false green" worry covered is now
  a real green. Repeatable: npm run typecheck:lens (local; needs the app).

[SESSION 5 — 2026-09-05] LENS STUDIO
  Lens Studio 5.23.2 (macOS arm) installed - the user had been running it
  from the mounted disk image; the app was copied to /Applications. A SPECS
  base-template project was created and saved as LensProject/ inside the
  repository (targetPlatform: Spectacles; SIK and SpectaclesUIKit packages
  included). The five scripts and twelve centimetre-authored models were
  copied into Assets/; the running editor imported all seventeen and wrote
  their .meta sidecars, and its own TypeScript compiler produced module
  output for all five scripts. Component wrappers (_c.js) appeared for two
  of four; SIK itself shows 10 wrappers for 22 components, so generation is
  lazy, not failed.
  Snap ships an MCP server and AGENTS.md with the SPECS template. Scene
  wiring is written as an Editor API script (Tools/lens-editor/) to run
  through ExecuteEditorCode, idempotent, verifying by reading each
  component's inputNames back. typecheck:lens now checks the gesture
  controller against the real SIK source from the project cache: clean.

[SESSION 5, LATER — 2026-09-05] THE LENS RUNS
  The editor had been on its Home screen with a temp "Default" project;
  every recompile had been hitting an empty project. Opened LensProject via
  Editor.Model.IModel.openProject through ExecuteEditorCode. Recompile then
  SUCCEEDED - the blocker had been a @hint given a concatenated string,
  which Lens Studio's decorator processor rejects and plain tsc accepts -
  and all four component wrappers appeared.
  wire-spatialis.ts ran clean after three defects of its own were fixed on
  live runs: 'declare' is illegal in the tool's function wrapper and the
  model comes from an injected pluginSystem; component proxies are fresh on
  every access, so duplicate detection must match by asset name from a
  single components read; and duplicates are removed with
  SceneObject.removeComponentAt, not destroy(). Result: one component per
  object, Anchor 5/5 inputs, Voice 10/10 with 12/12 prefabs, Gesture and
  Material missing only optional fields. Verified by API read-back and by
  the serialised Scene.scene: each script asset referenced once, no
  zero-GUID references.
  FIRST RUN IN PREVIEW (SPECS device simulation, 10s), verbatim from the
  Lens's own log: "[Spatialis:Material] 11 finishes, 10 tints loaded.",
  "[Spatialis:Anchor] Hit test session started (filtered).",
  "[Spatialis:Gesture] Tracking 2 hand(s).", "[Spatialis:Anchor] Floor
  estimated from eye height at y=-5.0cm.", "[Spatialis:Voice] Listening."
  No runtime errors. Two findings: VoiceML returned 401 - the Preview
  requires a My Lenses login for speech - and every VoiceML call used is
  flagged deprecated ("will stop functioning in an upcoming Lens Studio
  version"); migration to the current API is the next code change.
  One editor crash occurred earlier in the session (ES_ASSERT in
  MapOperations::get) while a diagnostic read scriptInputInfo on an
  uncompiled asset; that read is now avoided.

[SESSION 5, LATER STILL — 2026-09-05] ASR MIGRATION, SECOND PREVIEW RUN
  Every VoiceML call was deprecated per the editor's runtime warnings.
  VoiceCommandController now uses AsrModule (startTranscribing with
  AsrTranscriptionOptions; onTranscriptionUpdateEvent with isFinal;
  onTranscriptionErrorEvent). The migration was written only against
  members confirmed in StudioLib.d.ts, gated by the real-API type check
  (clean) and the 151 tests, then the editor recompiled (succeeded), the
  wiring script created and assigned the ASR Module asset, and the second
  Preview run showed: "[Spatialis:Voice] Transcribing.", NO deprecation
  warnings, and the new error hook firing with the expected Unauthenticated
  code because the Preview has no My Lenses login. The unused VoiceML asset
  was removed from the project.

[SESSION 5, FINAL — 2026-09-05] A SPOKEN COMMAND SPAWNS FURNITURE IN THE LENS
  The user spoke "Give me a navy velvet sofa." into the Preview. Verbatim
  from the Lens runtime: heard ... -> spawn sofa [velvet] {navy} @auto
  (confidence 1.00); No surface for sofa; floating it instead; sofa ->
  velvet; sofa tinted navy. Nothing appeared on screen. Three defects,
  found by querying the running scene through the MCP, fixed in order:
   1. IMPORT SCALE. The editor's glTF import applies "Convert meters to
      centimeters" by default (prefab root scale 100), so the centimetre
      set became a 21,000-unit sofa with the camera inside it. The project
      now imports the metre set: sofa 210 x 62 x 85 in the runtime scene.
   2. MATERIAL NAMES. glTF-imported materials expose baseColorFactor,
      metallicFactor and roughnessFactor; the swapper knew only the built-in
      PBR names, so its guarded writes skipped and "sofa -> velvet" changed
      nothing. It now speaks both vocabularies (two regression tests).
   3. FLOAT DISTANCE. A 2.1m sofa held 160cm from the eye filled the view
      with one face. Floating distance now scales with the piece and floated
      pieces avoid each other (tests updated and extended).
  A previewCommands input (empty in release) runs sentences on start so the
  pipeline can be verified in the Preview without a microphone. Final
  screenshot via MCP: a blue sofa behind a white marble table with a
  walnut-framed painting, none interpenetrating, all floating (no surfaces
  in the desktop Preview). 155 tests. The frontend and the Lens are the same
  Scripts/ files; sync-lens keeps the project copy identical.

[SESSION 5, GESTURES — 2026-09-05] THE GESTURE CONTROLLER ON THE RUNTIME
  With no Spectacles available, the gesture controller was exercised on the
  real runtime through Lens Studio's SPECS Preview simulation via the
  editor's PreviewInteractTool (simulated right hand: Pinch at the sofa,
  Drag +60cm over 2s, Release). Verbatim from the Lens log:
  "[Spatialis:Gesture] right hand grabbed sofa #1." then, on release,
  "[Spatialis:Gesture] Settled sofa on the unknown." Runtime scene: sofa x
  0 -> 61.25 while held -> 61.11 after release; surface stays "unknown"
  because the desktop Preview has no surfaces for reseat's probe to hit.
  This verifies the pinch hysteresis, target picking, drag and the release
  hand-off on the runtime. It does NOT verify feel on a real hand, nor
  anything on hardware; the README says so.

==============================================================================
 VERIFICATION SUMMARY (as of 2026-09-05)
==============================================================================
  npm run typecheck   -> clean (strict; against the local API stubs)
  typecheck:lens      -> clean against Snap's REAL API (Lens Studio 5.23.2
                         StudioLib.d.ts); mutation-verified
  npm test            -> 155 passed, 0 failed (7 suites, incl. end to end)
  npm run build:web   -> clean (simulator ES module bundle)
  fresh clone         -> npm ci, typecheck, test, build:web all green
  CI                  -> green on every push
  Lens Studio         -> 5.23.2; LensProject wired; RUNS in Preview - a spoken
                         command spawns and restyles furniture; not on device
  Simulator           -> hosts the real subsystems; verified via DOM, logs
                         and pixel sampling (image viewing unavailable to
                         the agent in session 4)
==============================================================================
